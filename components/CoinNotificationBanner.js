import React from "react";
import { StyleSheet, css } from "aphrodite";

const CoinNotificationBanner = (props) => {
  return <div className={css(styles.banner)}></div>;
};

const style = StyleSheet.create({
  banner: {},
});
