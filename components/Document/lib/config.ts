import globalColors from "~/config/themes/colors";

const config = {
  maxWidth: 900,
  background: "#FCFCFC",
  controlsWidth: 240,
  controlsWidthExpanded: 365,
  border: globalColors.GREY_LINE(1.0),
};

export default config;
