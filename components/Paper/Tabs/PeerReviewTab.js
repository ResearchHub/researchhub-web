import { StyleSheet, css } from "aphrodite";
import { connect } from "react-redux";

class PeerReviewTab extends React.Component {
  constructor(props) {
    super(props);
    this.state = {};
  }

  componentDidMount() {}

  componentDidUpdate() {}

  render() {
    return <div></div>;
  }
}

const styles = StyleSheet.create({});

export default connect()(PeerReviewTab);
